extends layout

block content
    section#profile
        #userInfos
            img#profilePicture(src=user.facebook.photo)
            p#userName #{user.facebook.name}
            p#userAge #{userAge} ans
        #buttons
            .demi-column.center
                a(class="button" id="settings" href="/profile/settings"): i.fas.fa-cog
                p Réglages
            .demi-column.center
                a(class="button" id="changeInfo" href="/profile/update"): i.fas.fa-pencil-alt
                p Modifier Vos Infos

